version: '3'

services:
  apiserver:
    build: ./docker/node
    image: express
    tty: true
    volumes:
      - ./app:/var/www/app
    ports:
      - 8080:3000
    environment:
      NODE_ENV: development
